import{ACCESS_CODE,adminCode,allowed,denied,memberToken,rpc}from"../../../lib";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){if(!await allowed())return denied();try{const{id}=await params;const body=await request.json();if(Object.hasOwn(body,"title")){const title=String(body.title||"").trim();if(!title)return Response.json({error:"Task title is required"},{status:400});if(title.length>200)return Response.json({error:"Task title is too long"},{status:400});const task=await rpc("nextasx_update_task_title",{p_token:await memberToken(),p_admin_code:await adminCode(),p_task_id:Number(id),p_title:title});return Response.json({task})}const task=await rpc("s2_update_task_status",{p_code:ACCESS_CODE,p_id:Number(id),p_status:body.status});return Response.json({task})}catch(e){console.error(e);return Response.json({error:"Update failed"},{status:500})}}
