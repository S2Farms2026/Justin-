import{adminCode,allowed,denied,memberToken,rpc}from"../../../../lib";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!await allowed())return denied();try{const{id}=await params;const{body}=await request.json();if(!String(body||"").trim())return Response.json({error:"Enter a checklist item"},{status:400});const item=await rpc("nextasx_add_checklist_item",{p_token:await memberToken(),p_admin_code:await adminCode(),p_task_id:Number(id),p_body:String(body)});return Response.json({item})}catch(e){console.error(e);return Response.json({error:"Checklist item could not be added"},{status:500})}}
